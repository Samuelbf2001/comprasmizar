# ESTADO — feat/pagos-nucleo-n4
Rama y base: feat/pagos-nucleo-n4 desde feat/pagos-nucleo @ 35aae5f (ola 1 integrada por el coordinador en main local c4c0d8d)
Último commit: N4.2 (este commit) — «Homónimos: la razón social solo es única entre empresas (NIT); dos personas con el mismo nombre y cédulas distintas conviven»; anteriores: a54ed75 (N4.1), 35aae5f (N3), a4a7e30 (N2), 82929d2 (N1).
Hecho:
- N4.1 (RF-008): migración `202609150004_obra_opcional_centros_no_obra.sql` + `supabase/tests/obra_opcional_centros_no_obra_verification.sql`: `gastos.obra_id` admite NULL (`drop not null`, con precedente en 202609010001/202609070003) y el trigger `gastos_obra_segun_centro` lo permite solo bajo un centro de costo cuyo tipo no sea `obra`. `requisiciones.obra_id` ya era nullable. Dominio: `costCenterRequiresWork(costCenter)`; `sendForApproval` exige obra solo con centro de tipo obra (o sin centro); `generateOrders` tolera obra ausente, el gasto nace con `workId: ""` (NULL en la base) y `billedCompanyId` vía `resolveBilledCompany(req, costCenter, work)`; `dashboardAggregates.expenseByWork` agrupa la obra NULL bajo `""`.
- N4.2 (RF-601): migración `202609150005_proveedores_homonimos.sql` + `supabase/tests/proveedores_homonimos_verification.sql`: `proveedores_razon_social_normalizada_unico_idx` se recrea con el MISMO nombre y expresión, parcial `where tipo_identificacion = 'NIT'`; `findSupplierDuplicate` recibe `identificationType/identification` y solo choca por nombre entre NIT (y por NIT o por (tipo, identificación normalizada)); `CatalogService.supplierConflict`, `SupplierService.create/resolveOrCreateBeneficiary` y `ProcurementService.create` con `beneficiary` aceptan homónimos con identificación distinta.
- N3 (docs/TASKS-pagos-y-caja.md §3): migración `202609150003_centro_costo_tipo_empresa_facturada.sql` + `supabase/tests/centro_costo_tipo_empresa_facturada_verification.sql` + par legado `.pre/.post`: `centros_costo.tipo` (obra/administrativo/personal/empresa, default obra); `requisiciones.empresa_facturada_id` (backfill = sociedad_id, trigger `requisiciones_1_empresa_facturada` que la deja por defecto en la sociedad, NOT NULL); `gastos.empresa_facturada_id` (instantánea, backfill desde requisición/obra); índices.
- Dominio: `CostCenterType`; `Requisition/Order/Expense/CashPayment.billedCompanyId`; `CatalogCostCenter.type`; `resolveBilledCompany(req, costCenter?, work?)`; `assertCanSelfApprove(actor)`.
- Servicio: `ReviewInput.billedCompanyId` (tres estados, validación de sociedad activa, default derivado una vez); evento `revisada` con `billedCompanyId` y `montoAntes/montoDespues` cuando cambia el valor en `tipo=pago`; `sendForApproval(id, ctx, { notifyApprovers? })` exige empresa facturada en `tipo=pago`; `generateOrders` copia `billedCompanyId` al gasto; `sendAndApproveAsMaster(id, ctx)` (dos eventos, sin aviso al aprobador, exige roles y ser el aprobador asignado salvo admin_sixteam).
- Repositorio/HTTP: `ListQuery.billedCompanyId` (requisiciones, órdenes, gastos; `parseListQuery`/`hasListFilters`); `orderSelectColumns` + `listCashPayments` exponen la empresa facturada; catálogos `costCenters` con `type` (create/patch/manage GET); acción `send_and_approve` y `review.billedCompanyId` en `requisitionActionSchema`/ruta de acciones.
- N2 (docs/TASKS-pagos-y-caja.md §3): migración `202609150002_proveedores_identificacion.sql` + `supabase/tests/proveedores_identificacion_verification.sql` + par legado `supabase/tests/legacy/202609150002_proveedores_identificacion.{pre,post}.sql`: `proveedores.tipo_identificacion` (NIT/CC/CE/PAS, default NIT), `identificacion` (+ `identificacion_normalizada` generada), `pendiente_normalizacion`; backfill `identificacion = nit`; índice único `(tipo, identificacion_normalizada)`; trigger `proveedores_identificacion` que espeja `nit` ⇄ `identificacion` (NIT) y deja `nit` NULL para personas; `auditoria_campo_sensible` redacta la identificación de proveedores.
- Dominio: `SupplierIdentificationType`, `Supplier.identificationType/identification/pendingNormalization`, `BeneficiaryInput`, `normalizeIdentification`.
- `SupplierService`: `SupplierWrite` con identificación; `resolveSupplierIdentity` (exportada); `findByIdentification(type, identification, actor)`; `resolveOrCreateBeneficiary({identificationType, identification, name, phone?}, actor)` → `{ supplier, created }` (crea `pendingNormalization: true`; permiso = administrar proveedores).
- `ProcurementService.create()` acepta `beneficiary` (RF-606) en `type: "pago"` como alternativa a `items[0].finalSupplierId`: enlaza por identificación o crea pendiente en la misma transacción (portal/WhatsApp sin permiso; web solo con `supplier:manage`). `CatalogRepository.findSupplierByIdentification`.
- HTTP: `createRequisitionSchema.beneficiary` (`beneficiarySchema`, `SUPPLIER_IDENTIFICATION_TYPE_VALUES`); `supplierCreateSchema`/PATCH de proveedor y catálogos (`kind: suppliers`) aceptan `identificationType/identification/pendingNormalization`; `GET /api/catalogs/manage` lista esos campos.
- PDF: `OrderDocumentSupplier.identificationType/identification`; el bloque BENEFICIARIO imprime `CC: …`/`NIT: …` (cae a `nit` legado).
- N1 (docs/TASKS-pagos-y-caja.md §3): migración `202609150001_pagos_anulacion_comprobante.sql` + `supabase/tests/pagos_anulacion_verification.sql` (registrado en `scripts/verify-schema.ts`): `pagos_orden.nota/anulado/motivo_anulacion/anulado_por/anulado_en`, trigger anti-sobrepago ignora anulados (y se dispara al reactivar), `pagos_orden` entra a `escribir_auditoria`, policy de UPDATE para anular, entidad `pago_orden` en `adjuntos` (check de entidad, path `pagos-orden/…`, metadata, existencia del padre, RLS de lectura y policy de Storage).
- Dominio: `PaymentStatus`, `paymentStatus(total, paid)`, `sumPaid(payments)` (ignora anulados), `assertCanAnnulPayment(payment, reason)`; `OrderPayment` gana `note/annulled/annulmentReason/annulledBy/annulledAt/attachmentId`; `Order` gana `paymentStatus/lastPaymentAt/paymentMethods`; `CashPayment`; `AttachmentEntity` + `pago_orden`.
- Servicio: `registerOrderPayment` acepta `note` y mide el saldo solo con pagos vigentes; `annulOrderPayment(orderId, paymentId, reason, ctx)`; `listCashPayments({from,to,costCenterId?}, ctx)`; `updateOrderAdminStatus(…, "pagada")` ya NO inventa el pago `otro`: exige saldo 0 (`SALDO_PENDIENTE`) y fija la fecha del último pago vigente en el gasto.
- Repositorio Postgres: lateral de pagos solo vigentes + `ultimo_pago`/`medios`/`gasto_total`; filtros de órdenes `costCenterId`, `paymentMethod`, `paymentStatus`, `paidFrom/paidTo` (`ListQuery`, `parseListQuery`, `hasListFilters`); `getOrderPayment`/`annulOrderPayment`/`listCashPayments`; `markExpensePaid(ref, null)` deshace la fecha de pago.
- Rutas: `POST /api/orders/[id]/payments` acepta `note`; nueva `PATCH /api/orders/[id]/payments/[paymentId]` con `{ action: "annul", reason }`; `/api/attachments/pago_orden/...` (prepare/complete/download/batch) para el comprobante.
- `components/screens/connected/payment-labels.tsx`: `MEDIO_PAGO_LABELS` (efectivo → "Caja (efectivo)"), `MEDIO_PAGO_OPTIONS`, `medioPagoLabel`, `PAYMENT_STATUS_LABELS`, `paymentStatusLabel`, `PaymentStatusBadge`.
A medias:
- Nada: N4 (dos puntos) completo sobre la ola 1.
Próximos pasos:
- Parche del coordinador (fuera de esta rama): `Expense.workId` → `workId?: string` y adaptar `app/api/reports/expenses-report.ts` (`workIdsForSociety.has(expense.workId)`, `work: expense.workId`) y `lib/reports/types.ts` (`ReportExpense.work?`); mientras tanto un gasto sin obra viaja con `workId: ""` y el filtro por sociedad del reporte lo deja fuera.
- Ola 2 (S1–S5): las pantallas que muestran la obra de un gasto/orden deben tolerar `workId` vacío/ausente (centros administrativos, personales o de empresa).
Cómo verificar:
- Cierre N4: `npm run lint && npm run typecheck && npm run test && npm run verify:schema`
- N4: `npm run test -- tests/unit/domain.test.ts tests/unit/procurement-service.test.ts tests/unit/postgres-repositories.test.ts supplier tests/unit/catalog-service.test.ts`
- Cierre de la ola 1: `npm run lint && npm run typecheck && npm run test && npm run verify:schema`
- N1: `npm run test -- tests/unit/domain.test.ts tests/unit/procurement-service.test.ts tests/integration/order-payments-route.test.ts`
- N2: `npm run test -- supplier tests/unit/procurement-service.test.ts tests/unit/order-document-pdf`
- N3: `npm run test` completo
- (también tocados) `npm run test -- tests/unit/postgres-repositories.test.ts tests/unit/attachment-service.test.ts tests/unit/http-api.test.ts tests/unit/catalog-service.test.ts tests/integration/routes.test.ts`
Decisiones pendientes del usuario:
- La identificación de un proveedor (cédula) se REDACTA en `auditoria` (`auditoria_campo_sensible`, N2) porque es dato personal; `nit` sigue visible como hasta hoy. Si contabilidad necesita ver el número en la traza, hay que quitar esa línea de la migración.
- Nombres en inglés en el dominio (`identificationType`, `identification`, `pendingNormalization`, `billedCompanyId` en N3) donde el plan escribía `tipoIdentificacion`/`empresaFacturadaId`: es la convención del resto del modelo (`societyId`, `costCenterId`). Columnas SQL sí en español.
- `sendAndApproveAsMaster` exige que el maestro figure como aprobador asignado (o sea admin_sixteam): si Daniel dejó a Juliana como aprobadora, el botón "Aprobar yo mismo" responde NOT_ASSIGNED_APPROVER en vez de saltársela. Alternativa: reasignar al propio actor dentro del mismo paso.
- (Resuelto en N4.1) Un centro `administrativo`/`personal`/`empresa` ya no exige obra. La regla "centro de tipo obra ⇒ obra obligatoria" vive en la base solo para `gastos` (trigger) y en el dominio para `requisiciones` (`sendForApproval`/`generateOrders`): una requisición pasa por estados intermedios legítimos con centro y sin obra (portal antes de la revisión, borradores), así que un trigger sobre `requisiciones` rompería ese flujo. `caja_menor.obra_id` sigue NOT NULL (módulo dormido, A10).
- N4.2 recrea un índice único con `drop index if exists` + `create unique index` (mismo nombre). No hay precedente literal de `drop index` en el repo; sí de recrear por nombre triggers, policies y constraints. Un índice único no se puede desactivar de otra forma. Si esa lectura de la convención "sin DROP" no convence, la alternativa es dejar el índice viejo y aceptar que dos personas homónimas no caben.
- La migración de homónimos usa el índice `202609150005`, que el plan no había reservado (N4 solo reservaba `…0004`); ninguna otra rama de la ola 2 crea migraciones.
- La empresa facturada explícita puede ser cualquier sociedad ACTIVA, sin exigir que coincida con la de la requisición/obra (el centro de costo sí lo exige): es el punto de RF-009. Si contabilidad quiere acotarla a un conjunto, es una regla nueva.
- Anular el pago que cerraba una orden ya marcada "pagada" (eje administrativo) la DEVUELVE a "contabilizada" y borra la fecha de pago del gasto (`annulOrderPayment`). Es la única reversa de `assertAdminTransition`; el plan no la fija y el PRD (E2E #6, "vuelve a parcial") la supone. Alternativa: prohibir anular sobre una orden ya "pagada".
- El comprobante NO viaja como `attachmentId` en el POST del pago (el plan lo listaba): un adjunto exige que su padre exista, así que se sube DESPUÉS contra el id del pago (`/api/attachments/pago_orden/<paymentId>`), y `OrderPayment.attachmentId` es de lectura (el adjunto más reciente).
- `updateOrderAdminStatus('pagada')` con saldo lanza `DomainError("SALDO_PENDIENTE")` (422) en vez de devolver `{ok:false, reason:'saldo_pendiente'}`: es el idioma del resto del servicio y de `apiError`.
- Registrar el pago que cubre el saldo NO cierra solo el eje administrativo (`adminStatus` sigue en pendiente/contabilizada); S1 debe encadenar `PATCH adminStatus=pagada` tras "Pagar saldo" si quiere ese atajo. `paymentStatus` (derivado) sí pasa a "pagada" de inmediato.
Riesgos:
- Hasta que S1 sustituya "Marcar pagada" por "Pagar saldo", el botón actual de `orders.tsx` fallará con 422 `SALDO_PENDIENTE` en cualquier orden con saldo (comportamiento buscado por A3, pero visible en `main` si se integra la ola 1 sola).
- `lib/http/api.ts`, `app/api/attachments/**` y `tests/unit/{postgres-repositories,attachment-service,http-api,security}.test.ts` + tres `tests/integration/*` se tocaron fuera de la lista de N1 (fakes de `OrderPaymentRepository`, enums de entidad, parseo de los filtros nuevos): sin ellos los filtros y el comprobante no llegan a la ruta HTTP.
- `paymentStatus` en SQL (filtro) y en TS (`paymentStatus()`) son dos copias de la misma regla; si cambia una, cambia la otra (comentado en ambos sitios).
- (Resuelto en N4.2) `razon_social` es única solo entre NIT; dos personas homónimas con cédulas distintas conviven. Ojo: `lower()` de la base depende del locale de producción para nombres con tildes en mayúsculas ("JUAN PÉREZ" vs "Juan Pérez" chocan en UTF8 con locale real, no bajo LC_CTYPE 'C').
- `Expense.workId` sigue tipado `string` y un gasto sin obra viaja como `""`: es un sentinel deliberado y temporal hasta el parche del coordinador en el reporte (ver Próximos pasos). `groupExpenseByWork`/`expenseByWork` lo agrupan bajo `""`, igual que etiquetas/centros sin valor.
- `nit` y `identificacion` conviven espejadas por trigger; el importador (`scripts/import-master-data.ts`) sigue escribiendo solo `nit` y funciona por ese espejo, pero solo para tipo NIT.
- `gastos.empresa_facturada_id` es NULLABLE y los gastos de caja menor NUEVOS (`sincronizar_gasto_caja_menor`, módulo que A10 retira) nacen sin ella; el backfill sí cubrió los históricos. Si el módulo sobreviviera, el trigger debería copiar la sociedad de la obra.
- Con cada autoguardado, `review()` puede hacer hasta dos lecturas extra de catálogo la PRIMERA vez que deriva la empresa facturada (centro y obra); después queda guardada y no se recalcula.


---

# ESTADO — feat/ordenes-pagos-ui
Rama y base: feat/ordenes-pagos-ui desde feat/pagos-nucleo @ 35aae5f (ola 1 completa; esa rama sale de origin/main @ f9a7e00)
Último commit: S1 (este commit) — «Órdenes: diálogo de pago con medio, nota y comprobante, "Pagar saldo" en vez de "Marcar pagada" con saldo, historial con anulados y anulación con motivo, badge de estado de pago y filtros de pago del servidor»
Hecho:
- S1 (docs/TASKS-pagos-y-caja.md §4), solo `components/screens/connected/orders.tsx` + `tests/unit/connected-orders-*.test.tsx`:
  - Diálogo de pago (`OrderPaymentDialog`, mismo patrón de capas que `useConfirmDialog`: `.quick-supplier-overlay` sobre `.supplier-overlay`): fecha (hoy por defecto), valor, medio (`MEDIO_PAGO_OPTIONS`, "Caja (efectivo)"), referencia, nota; validación inline (fecha, valor > 0, valor ≤ saldo, medio obligatorio) con `aria-invalid`/`aria-describedby`; Escape, trampa de foco y foco devuelto al disparador. `POST /api/orders/[id]/payments` con `note`/`externalReference` solo si tienen valor.
  - Paso 2 opcional «Adjuntar comprobante»: `uploadSignedAttachment` (attachment-upload.tsx) contra `/api/attachments/pago_orden/<paymentId>` (prepare) y `/…/<attachmentId>/complete`, tipo `soporte`. El historial ofrece «Adjuntar» al pago vigente sin comprobante (abre el mismo diálogo en el paso 2) y «Descargar» (`/api/attachments/pago_orden/<paymentId>/<attachmentId>/download`) cuando `attachmentId` existe.
  - «Pagar saldo» (A3): con `adminStatus=contabilizada` y saldo > 0 sustituye a «Marcar pagada»; abre el diálogo prellenado con el saldo. Tras cualquier pago que deje el saldo en 0 (según `order.paymentStatus` de la respuesta, o `paidAmount` vs total si falta), si `canPay` y la orden está contabilizada, se encadena `PATCH adminStatus=pagada` sin segundo diálogo. «Marcar pagada» (PATCH directo, con confirmación) solo queda cuando el saldo ya es 0. No existe ningún botón que mande `adminStatus=pagada` con saldo > 0. «Registrar pago» se deshabilita (con `title`) cuando no queda saldo.
  - Historial: anulados con `<del>` (semántico, sin CSS nuevo), badge «Anulado» y una fila `.muted-copy` con «Anulado el <fecha> por <usuario> (`resolveUserName`): motivo» y «Nota: …». «Anular» (`.text-link`, solo pagos vigentes y `canRegisterPayment`) → `confirm()` con `reason` obligatorio → `PATCH /api/orders/[id]/payments/[paymentId] { action: "annul", reason }`; luego recarga historial y lista.
  - `PaymentStatusBadge`: columna «Pago» en la lista (— si la fila no trae `paymentStatus`) y tercer eje «Pago» en la ficha junto a Entrega/Contabilidad; la ficha también muestra «Empresa facturada» y «Último pago».
  - Filtros de pago en una segunda `.filter-bar` (la primera ya desborda en escritorio con seis campos y dos atajos): medio de pago, estado de pago, centro de costo, empresa facturada (`catalogs.societies`), pagado desde/hasta. Los resuelve el servidor: la pantalla pide `/api/orders?paymentMethod=&paymentStatus=&costCenterId=&billedCompanyId=&paidFrom=&paidTo=` (nombres de `parseListQuery`) mientras alguno tenga valor y usa esas filas como base; los filtros de siempre (obra, entrega, contabilidad, proveedor, fecha requerida) siguen en cliente encima. Respuestas tardías se descartan comparando la query vigente en el updater (sin refs, por `react-hooks/refs`). Tras cada mutación se recarga también la lista filtrada (`reloadList`).
  - Pruebas: `tests/unit/connected-orders-pagos.test.tsx` (15 casos: badge, diálogo/POST/etiqueta de caja, validación, Pagar saldo encadena solo si contabilizada y con `canPay`, saldo 0 → Marcar pagada, anulados tachados, Anular con motivo, aprobador sin acciones, comprobante prepare/complete, enlace de descarga, filtros con los seis params exactos, filtros de cliente encima y Limpiar). `connected-orders-admin` (etiqueta «Caja (efectivo)», flujo por diálogo «Registrar pago» → «Guardar pago») y `connected-orders-cost-center` (`within(fila)` porque «Pago» es ahora también encabezado) actualizados; `connected-orders-lente` pasa sin cambios (frases conservadas).
A medias:
- Nada.
Próximos pasos:
- Coordinador: parche sobre `main` (abajo) para retirar los tipos locales de orders.tsx cuando `shared.tsx` gane los campos.
- QA visual (ola 3): ficha con un pago anulado + badge; filtros de pago combinados con los de siempre; el diálogo en 375 px (`.quick-supplier-dialog` es `min(100%,480px)`).
Cómo verificar: `npm run typecheck` · `npm run test -- tests/unit/orders tests/unit/connected-orders` (lista `connected-orders-admin`, `connected-orders-cost-center`, `connected-orders-lente`, `connected-orders-pagos`; 32 pruebas) · `npm run lint` · también verdes `npm run test -- tests/unit/connected-filters tests/unit/order-detail-total`, que renderizan la misma pantalla.
Parche para el coordinador (fuera de mis archivos, no lo toqué):
- `components/screens/connected/shared.tsx`: `OrderRow` debe ganar `paymentStatus?`, `lastPaymentAt?`, `paymentMethods?`, `billedCompanyId?` y `OrderPaymentRow` debe ganar `note?`, `annulled?`, `annulmentReason?`, `annulledBy?`, `annulledAt?`, `attachmentId?` (mismos nombres que `Order`/`OrderPayment` en lib/domain/model.ts). Hoy viven como `OrderListRow`/`PaymentRow` locales en orders.tsx; al integrar, borrar esos dos tipos y usar los de shared. `uploadOperationalAttachment` tampoco admite la entidad `pago_orden` (por eso el diálogo llama a `uploadSignedAttachment` directo); añadirla al union de `entity` permitiría reutilizarla.
- `components/screens/connected/data.ts` (opcional): `loadRoute("orders")` no conoce los filtros de pago, así que la lista filtrada no pasa por la caché de ruta ni por sessionStorage; la pantalla la pide y la recarga por su cuenta. Si se quiere una sola fuente, `loadRoute` tendría que aceptar la query.
- CSS (opcional, no bloquea): `.filter-bar` solo envuelve bajo el media query de tablet (globals.css:833); en escritorio la segunda barra de seis campos cabe, pero un ancho intermedio (~1100 px con sidebar) la recorta. Una regla `flex-wrap:wrap` general resolvería ambas barras.
Decisiones pendientes del usuario:
- El encadenado a "pagada" se aplica a CUALQUIER pago que deje el saldo en 0 (desde «Pagar saldo» o desde «Registrar pago»), siempre que quien registra tenga `order:pay` y la orden esté contabilizada — PRD §4.4: «Daniel no marca pagada: registra pagos y la orden se marca sola». Contabilidad (sin `order:pay`) registra el pago y la orden queda `paymentStatus=pagada` pero `adminStatus=contabilizada`, a la espera del revisor. Si se prefiere encadenar solo desde «Pagar saldo», es pasar el `mode` a `registerPayment`.
- «Anular» se ofrece también en órdenes ya marcadas «pagada» (la ola 1 decidió que el servidor las devuelve a «contabilizada»); el diálogo lo avisa.
- Encabezado de columna «Pago» para el tercer eje (en vez de «Estado de pago»), a juego con «Entrega»/«Contabilidad».
- `costCenterId` se filtra en el servidor (como pide el plan) aunque `row.costCenterId` ya viaja en la fila y podría filtrarse en cliente.
Riesgos:
- La lista filtrada por el servidor no se invalida por mutaciones hechas en otras pantallas (la caché de ruta sí); al volver a /ordenes el estado de la pantalla se reinicia, así que el desfase dura como mucho la visita.
- Si `/api/orders` filtrado fallara (400 por un uuid inválido, p. ej. un `costCenterId` que no sea uuid), la pantalla muestra el error y conserva las filas anteriores; los valores salen de `catalogs`, así que en la práctica siempre son uuid.
- `AttachmentPicker` limita a 10 MB (MAX_ATTACHMENT_BYTES) mientras `attachmentUploadSchema` admite 20 MB: discrepancia previa a esta rama, no la toqué.
- La tabla pasa de 12 a 13 columnas; ya desplazaba en horizontal dentro de `.table-wrap`, así que no cambia el comportamiento en móvil.


---

# ESTADO — feat/flow-pago
Rama y base: feat/flow-pago desde feat/pagos-nucleo @ 35aae5f (ola 1 verificada; origin/main @ f9a7e00)
Último commit: (este commit) — «WhatsApp Flow de solicitud de pago: tercer Flow con beneficiario por identificación, opción "Solicitar un pago" en el menú del bot, y el Flow de captura deja de ofrecer un tipo pago que no funcionaba»
Hecho:
- S4 (docs/TASKS-pagos-y-caja.md §4, A11 / RF-908 / RF-902 modificado):
- `scripts/build-flow-pago.ts` (nuevo) genera `integrations/whatsapp-flow/solicitud-pago.flow.json`: 3 pantallas sin PhotoPicker — BENEFICIARIO (tipo de identificación CC por defecto/NIT/CE/PAS + número + nombre o razón social), PAGO (empresa que paga = `data.sociedades`, con nombre como valor igual que en captura; monto en pesos solo dígitos; concepto), RESUMEN (rótulos estáticos + bindings puros; única concatenación `` `${data.tipo_identificacion} ${data.identificacion}` ``; `complete` con `kind: "pago"`). Reutiliza `textoRecibido`/`listaRecibida` (ahora exportadas) y las reglas del README.
- `lib/infrastructure/flow-sender.ts`: `sendPaymentFlow(to, deps)` con env `WHATSAPP_FLOW_PAGO_ID` (+ `_CTA`, `_BODY`, `_MODE`), `buildPaymentFlowSendPayload`, `PAYMENT_FLOW_ENTRY_SCREEN`, `isPaymentFlowConfigured`; falla cerrado con `PAYMENT_FLOW_NOT_CONFIGURED`. Mismo `flow_token` que captura (`issueFlowToken`). La firma de `sendRequisitionFlow` no cambia; el POST al proxy quedó en `postFlowMessage`, compartido. Texto por defecto del Flow de captura ya no dice «o pagos».
- `lib/infrastructure/whatsapp-router.ts`: tercer botón `mizar_solicitar_pago` («Solicitar un pago», 17 caracteres; tres es el tope de WhatsApp), acción `flow_pago` registrada en `whatsapp_eventos` como las demás, `RouterDeps.enviarFlowPago`; saludo neutro; «Mis requisiciones» ya listaba todos los tipos (no filtra) y ahora marca `Pago ·` las de tipo pago (`RequisicionResumen.tipo`).
- `lib/infrastructure/payment-reply-adapter.ts` (nuevo): `isPaymentNfmReply` (discriminador `kind: "pago"`), `adaptPaymentReply` (token → forma → `invalid_amount` → empresa por nombre → lista blanca) → `KapsoWebhookEvent` con `type: "pago"`, `beneficiary {identificationType, identification, name, phone = remitente}` e `items: [{quantity: 1, unit: "unidad", proposedDescription: concepto, unitBase: monto}]`. `parsePaymentAmount` acepta puntos de miles.
- `app/api/kapso/route.ts`: rama del Flow de pago antes de la de captura (rechazo neutro 200 + `createPostgresNfmReplyRejectionRecorder`, igual que captura); `kapsoWebhookSchema` acepta `beneficiary` (`beneficiarySchema`) y `items[].unitBase`, y para `type: "pago"` exige beneficiario y exactamente un ítem con `unitBase > 0`; `creator.create` pasa `beneficiary` y `unitBase` a `ProcurementService.create` (ola 1 enlaza o crea el proveedor pendiente en la misma transacción).
- `lib/services/kapso-contracts.ts` (el plan lo nombra en `lib/infrastructure/`, pero vive en `lib/services/`; es el contrato del canal, no un servicio): `KapsoFlowItem.unitBase?`, `KapsoFlowSubmission.beneficiary?: BeneficiaryInput`.
- `scripts/build-flow-captura.ts` + JSON regenerado: se retira `tipo_solicitud` (pantalla, `data`, reenvíos y resumen); el `complete` manda `type: "compra"` fijo.
- `lib/infrastructure/nfm-reply-adapter.ts` (fuera de la lista, es el «parseo asociado» que pide el paquete): `extractTopLevelFields` solo acepta `type: "compra"`; un `pago` del Flow v3 publicado se rechaza como `invalid_fields` (antes: `PAYMENT_BENEFICIARY_REQUIRED` → 503 → reintentos de Kapso).
- `scripts/publish-whatsapp-flow.ts` (fuera de la lista, sin conflicto con S1–S5): entrada `pago` («Solicitud de pago – Mizar») y `requisicion` pasa a «Requisición de obra – Mizar v4» (el v3 está publicado y no se puede editar).
- `.env.example` (bloque `WHATSAPP_FLOW_PAGO_*`), `integrations/whatsapp-flow/README.md` (sección «WhatsApp Flow — Solicitud de pago», v4 de captura, tabla de mapeo).
- Pruebas: `fixtures/nfm-reply-pago.json`; `tests/integration/kapso-pago.test.ts` (adaptador puro, ruta HTTP: crea tipo pago con beneficiario pendiente / reutiliza por identificación / sin monto → `rejected:invalid_amount` sin crear requisición ni proveedor / no autorizado / duplicado / captura vieja con `type: pago` → `invalid_fields` / esquema normalizado 400; estructura del Flow JSON y cruce `complete` ⇄ `CAMPOS_PAGO_LEIDOS`); `tests/unit/whatsapp-router.test.ts` (tres botones, el tercero envía el Flow de pago con `WHATSAPP_FLOW_PAGO_ID` y pantalla BENEFICIARIO, fallo cerrado sin la variable, `Pago ·` en la lista).
A medias:
- Nada del paquete. La validación remota de Meta y la publicación son pasos manuales (abajo).
Próximos pasos:
- Coordinador: `npx tsx --env-file=.env.local scripts/publish-whatsapp-flow.ts pago` → pegar `validation_errors` (debe ser `[]`); cargar `WHATSAPP_FLOW_PAGO_ID=<flow_id>` y `WHATSAPP_FLOW_PAGO_MODE=draft`; recorrer la vista previa en un teléfono; publicar (`POST /{FLOW_ID}/publish?business_account_id=$KAPSO_WABA_ID`, README) y quitar `_MODE`.
- Coordinador: lo mismo para el Flow de captura v4 (`npx tsx --env-file=.env.local scripts/publish-whatsapp-flow.ts`), y al publicarlo apuntar `WHATSAPP_FLOW_ID` al nuevo id. Mientras tanto el v3 sigue en producción y su opción «pago» recibe un rechazo neutro (registrado), no un 503.
- Producción: `WHATSAPP_FLOW_PAGO_ID` en el env del servicio (EasyPanel). Sin ella el botón queda como `error:PAYMENT_FLOW_NOT_CONFIGURED` en `whatsapp_eventos`.
Cómo verificar:
- `npm run typecheck` · `npm run lint`
- `npm run test -- tests/integration/kapso tests/unit/whatsapp-router` (incluye `kapso-attachments`, `kapso-idempotency`, `kapso-pago`, `whatsapp-router`)
- `npx tsx scripts/build-flow-pago.ts --check` y `npx tsx scripts/build-flow-captura.ts --check` (JSON al día)
- También tocados: `npm run test -- tests/integration/nfm-reply tests/unit/flow-captura-v2 tests/unit/flow-sender tests/unit/send-flow-route tests/unit/nfm-reply-society-resolver`
- Validación remota de Meta (manual, requiere `KAPSO_API_KEY` y `KAPSO_WABA_ID` en `.env.local`):
  `npx tsx --env-file=.env.local scripts/publish-whatsapp-flow.ts pago` → esperado `"validation_errors": []`
  Vista previa: `curl -s "https://api.kapso.ai/meta/whatsapp/v24.0/<FLOW_ID>?fields=preview.invalidate(false),status,validation_errors&business_account_id=$KAPSO_WABA_ID" -H "X-API-Key: $KAPSO_API_KEY"`
Decisiones pendientes del usuario:
- El teléfono del remitente se guarda como contacto del beneficiario cuando nace pendiente (quien pide el pago por WhatsApp casi siempre es quien cobra). Si Daniel prefiere que un tercero nazca sin teléfono, es quitar `phone` en `adaptPaymentReply`.
- El monto en el Flow admite solo dígitos (`^[1-9][0-9]{0,11}$`, sin puntos): el patrón lo valida el teléfono antes de enviar. El adaptador sí tolera puntos de miles por si otro emisor los manda.
- El Flow de captura pasa a v4 al retirar el tipo: exige crear un Flow nuevo en Meta y cambiar `WHATSAPP_FLOW_ID`. Alternativa si no se quiere republicar ya: dejar el v3 (la opción «pago» ahora se rechaza limpio) y publicar el v4 más adelante.
- «Mis requisiciones» sigue diciendo «requisiciones» en el botón (17 caracteres; «Mis solicitudes» también cabe). El cuerpo ya dice «solicitudes».
Riesgos:
- La validación remota de Meta no se corrió (sin credenciales aquí): `init-value` en `RadioButtonsGroup` y `pattern` en `TextInput` de tipo texto ya se usan en los Flows validados (aprobación y captura), pero este JSON concreto no ha pasado por `validation_errors` todavía.
- Un rechazo del adaptador (monto inválido, número no autorizado) responde 200 a Kapso y NO le dice nada a la persona, igual que en captura: el formulario «se pierde» desde su punto de vista. Está registrado en `whatsapp_eventos`.
- `sendPaymentFlow` es un mensaje interactivo: solo sale dentro de la ventana de 24 h. Aquí siempre se cumple (responde a un botón que la persona acaba de pulsar).
- Hasta que se publique el v4 de captura, `WHATSAPP_FLOW_ID` apunta al v3 publicado, cuyo JSON ya no coincide con `requisicion-captura.flow.json` del repo (solo difiere en el tipo).


---

# ESTADO — feat/captura-pago-ui
Rama y base: feat/captura-pago-ui desde feat/pagos-nucleo @ 35aae5f (ola 1 verificada; merge-base con origin/main = f9a7e00 + N1/N2/N3)
Último commit: (ver `git log -1`) — «Captura y revisión de pagos: fecha del gasto, beneficiario por identificación, empresa facturada, valor original y "Aprobar yo mismo"; tipo en centros de costo e identificación en proveedores»; anterior: «Alta rápida de proveedor unificada (nombre, tipo de identificación, identificación, teléfono, correo) para captura, revisión y directorio»
Hecho:
- S2 (docs/TASKS-pagos-y-caja.md §4, fila S2). Solo archivos del paquete: `components/screens/supplier-quick-create.tsx` (nuevo), `components/screens/connected/new-requisition.tsx`, `components/screens/connected/detail.tsx`, `components/screens/catalog-admin.tsx`, `components/screens/suppliers.tsx`, y sus pruebas en `tests/unit/` (`supplier-quick-create.test.tsx` y `connected-detail-pago.test.tsx` nuevos; `connected-new-requisition`, `connected-review`, `catalog-admin`, `suppliers` ampliados/ajustados).
- `supplier-quick-create.tsx`: diálogo `SupplierQuickCreate` (razón social, tipo de identificación NIT/CC/CE/PAS, identificación, teléfono, correo, casilla "Ficha pendiente de completar" marcada por defecto) contra el MISMO endpoint que usaban las tres pantallas (`POST /api/suppliers` vía `mutate`, que además invalida la caché de catálogos). Helpers exportados: `identificationLabel`, `matchesIdentification`/`findSupplierByIdentification` (misma normalización que la base: `normalizeIdentification`), `lookupSupplierByIdentification` (GET /api/suppliers + filtro en cliente), `quickSupplierPayload`, `validateQuickSupplierDraft`, `createQuickSupplier`; prop `create` para el modo demo del directorio. Foco al abrir, trampa de Tab, Escape, devuelve el foco al disparador, doble clic no duplica el POST.
- `new-requisition.tsx` (tipo pago): "Fecha del gasto" (mismo `requiredDate`, hoy por defecto al cambiar a pago); soporte "Factura o cuenta de cobro (opcional)"; beneficiario por identificación (tipo + número + "Buscar beneficiario": si existe se enlaza al selector, si no se abre el alta rápida prellenada y el creado queda seleccionado) además del selector; el alta inline anterior se retiró. Tipo compra: sin cambios.
- `detail.tsx` (revisión): selector "Empresa facturada" (sociedades activas de `catalogs.societies`; inicial = `requisition.billedCompanyId` del servidor o, si falta, la derivación del dominio: sociedad del centro de costo → obra → requisición) que viaja como `billedCompanyId` en `review()` (autoguardado y "Enviar a aprobación") y se muestra en la cabecera de solo lectura; en `tipo=pago` la columna se rotula "Valor" y aparece "Valor original" en la barra de totales cuando difiere (fuente: `montoAntes` del PRIMER evento `revisada` del historial, si no el total tal como llegó del servidor) y en la ficha de solo lectura; botón "Aprobar yo mismo" junto a "Enviar a aprobación" con confirmación: guarda `review` con `approverId = data.viewerId` (sin tocar el `<select>`) y luego `send_and_approve` (antepone `start_review` en `enviada`). Visible solo si `role === "Administrador Sixteam"` o `data.viewerRoles` incluye `revisor` y `aprobador` (ver parche). El alta rápida por fila usa el diálogo unificado ("Crear y asignar").
- `catalog-admin.tsx`: pestaña Centros de costo con `tipo` (select obra/administrativo/personal/empresa, obra por defecto; columna "Tipo"); pestaña Proveedores con tipo de identificación + identificación (sustituyen al campo NIT; `nit` ya no viaja, lo espeja el servicio) y casilla "Pendiente de completar" (columna con badge).
- `suppliers.tsx`: columna y ficha "Identificación" (tipo + número, cae al `nit` legado), marca "Pendiente de completar" (tabla y ficha), búsqueda por identificación, edición con tipo/identificación/pendiente; "Nuevo proveedor" abre el diálogo unificado y, al crear, abre la ficha (demo: alta local vía prop `create`).
A medias:
- Nada del alcance de S2.
Próximos pasos:
- Coordinador: aplicar el parche de abajo para que "Aprobar yo mismo" sea visible al usuario maestro (hoy solo lo ve la lente Administrador Sixteam).
- Ola 3: E2E #5/#7 de la adenda pasando por la captura interna de pago y "Aprobar yo mismo".
Cómo verificar:
- `npm run typecheck` → 0 errores; `npm run lint` → 0 problemas
- `npm run test -- connected-new-requisition connected-detail catalog-admin suppliers supplier-quick-create` (además `connected-review`, que prueba el alta rápida desde la fila de revisión)
- Casos nuevos: alta rápida por identificación (captura y directorio), "Aprobar yo mismo" guarda `approverId` propio antes de `send_and_approve` y solo aparece con los roles correctos, valor original visible al editar (y desde `montoAntes` tras recargar), empresa facturada en `review`, tipo de centro de costo, identificación en catálogos.
Parche para el coordinador (fuera de los archivos del paquete):
- `app/api/requisitions/[id]/detail/route.ts`: devolver también `viewerRoles: actor.roles` junto a `viewerId`, y declarar `viewerRoles?: string[]` en `DetailBundle` (`components/screens/connected/shared.tsx`). `detail.tsx` ya lo lee como opcional: sin él, "Aprobar yo mismo" no aparece para un revisor+aprobador (la lente `role` no distingue roles múltiples) y solo lo ve Administrador Sixteam.
- `components/screens/connected/shared.tsx`: `RequisitionRow.billedCompanyId?: string` y `CatalogData.costCenters: Array<NamedOption & { societyId?: string }>` (el servidor ya los manda; `detail.tsx` los lee con tipos locales `BilledCompanyAware`/`CostCenterOption` que se pueden borrar al declararlos ahí).
- Opcional: ruta HTTP de búsqueda por identificación (`SupplierService.findByIdentification` existe sin ruta). Hoy la captura descarga el directorio (`GET /api/suppliers`) y filtra en cliente; con muchos proveedores conviene `GET /api/suppliers?identificationType=CC&identification=…`.
- `createRequisitionSchema` no acepta `costCenterId` ni `billedCompanyId`: en la captura de pago NO se muestran (el pie lo dice: se asignan en la revisión). Si RF-107 los quiere en la captura, hace falta ampliar `create()` + esquema (fuera de S2).
Decisiones pendientes del usuario:
- El alta rápida marca `pendingNormalization: true` por defecto (casilla visible, desmarcable): una alta con nombre + identificación no trae datos bancarios ni documentos. Si Daniel prefiere que sus altas internas nazcan "completas", basta invertir el default en `emptyDraft`.
- Directorio de proveedores: "Nuevo proveedor" ya no abre el formulario completo (datos bancarios + documentos en el mismo paso) sino el diálogo unificado y luego la ficha; la carga de documentos "al crear" (pendingDocuments) se retiró porque dejó de ser alcanzable. Reversible por git si se prefiere el formulario completo para el alta.
- "Valor original" en `tipo=pago` se muestra a nivel de TOTAL (base + IVA), igual que `montoAntes/montoDespues` del evento `revisada`, no a nivel de precio unitario.
- La empresa facturada se muestra y edita en la revisión de AMBOS tipos (RF-009 es para toda requisición), aunque el servicio solo la exige en `tipo=pago`.
Riesgos:
- `tests/e2e/suppliers.spec.ts` ("permite alta…"): el flujo sigue siendo botón "Nuevo proveedor" → diálogo "Nuevo proveedor" → "Razón social" → "Crear proveedor" → status "Proveedor creado correctamente" (mismos nombres accesibles), pero ahora hay un GET de la ficha tras el POST; no se ejecutó aquí (Playwright, requiere servidor).
- `lookupSupplierByIdentification` descarga todo el directorio en cada búsqueda; aceptable para el tamaño actual del catálogo, ver parche opcional.
- En `detail.tsx`, si el visor es Administrador Sixteam pero NO está en `catalogs.approvers` (no elegible), `review` con `approverId = actor` fallaría en el servidor (isEligibleApprover); el mensaje de error se muestra en pantalla, no hay estado inconsistente (la revisión no se envía).
- `catalog-admin.tsx` deja de mandar `nit` en proveedores: el servicio lo espeja desde `identification` cuando el tipo es NIT (N2). Un proveedor persona (CC) queda con `nit = NULL` a propósito.


---

# ESTADO — feat/portal-pago
Rama y base: feat/portal-pago desde feat/pagos-nucleo @ 35aae5f (ola 1 verificada; esa rama nace de origin/main @ f9a7e00)
Último commit: (este commit) — «Portal público: «Solicitud de pago» abre un camino de tres pasos —quién cobra, el pago, resumen— y el formulario muestra el motivo cuando el servidor rechaza»; anterior: a7ec196 (endpoint: unión por `type`, 400 con motivo, 202 neutro solo para contraseña/enlace/límites).
Hecho:
- S3 (docs/TASKS-pagos-y-caja.md §4, RF-108 / A12). Archivos: `app/api/public/requisitions/route.ts`, `components/screens/public-request.tsx`, `tests/unit/public-portal-hardening.test.ts`, `tests/unit/public-request-portal.test.tsx`, `tests/integration/public-payment.test.ts` (nuevo), `tests/integration/public-photos.test.ts` (413), `tests/e2e/public-portal.spec.ts`.
- API: `publicRequisitionSchema` es `z.discriminatedUnion("type", [compra, pago])` + refine «workId o societyId, exactamente uno». Rama `pago`: `beneficiary {identificationType NIT|CC|CE|PAS, identification 3..32, name 2..160}`, `amount` entero > 0 (≤ `MAX_PUBLIC_PAYMENT_AMOUNT` = 1e12, cabe en `numeric(16,2)`), `concept` 1..`MAX_PUBLIC_PAYMENT_CONCEPT_LENGTH` (120), `phone?`, `observations?`; sin `items` ni `name` (quien radica ES el beneficiario). Crea vía `create({ type: "pago", beneficiary: {…, phone normalizado}, externalRequester: { name: beneficiary.name, phone }, items: [{ description: concept, quantity: 1, unit: "servicio", unitBase: amount, unitIva: 0 }] })`.
- API, respuestas: 400 `invalid_input` (+`issues[{path,code}]`, mismo contrato que `apiError`), 400 `invalid_json`/`invalid_multipart`, 413 `payload_too_large`, `DomainError` tras verificar → 409 CONFLICT / 403 FORBIDDEN / 413 / 422 resto con `{error, message}`, 503 `service_unavailable` para infraestructura (deps, verify que revienta, `create` que revienta) y para `PUBLIC_ACCESS_DENIED` lanzado por el servicio (inconsistencia interna, ver Riesgos). 202 neutro SOLO: `publicFormRateLimiter`, limitadores por destino, `verify`/`verifySociety` falsos. La foto se guarda en un try propio: nunca convierte una radicación válida en error (evita el reenvío duplicado).
- UI: fases nuevas `beneficiario` y `pago`; `ETAPAS_PAGO` (Solicitud → Quién cobra → El pago → Resumen) vs `ETAPAS_COMPRA` (5); `Progress` recibe las etapas y fija las columnas en línea (el módulo CSS trae 5 fijas). Paso 1 con «Solicitud de pago»: sin selector de empresa (va al paso «El pago», A12) y una pista de qué se va a pedir. «¿Quién cobra?»: tipo (select, default CC), número (quita puntos/espacios al teclear; `^[0-9A-Za-z-]{3,32}$`), nombre completo/razón social, teléfono opcional (misma regla que compra). «El pago»: `SelectorEmpresa` rotulado «Empresa a la que cobras», monto (solo dígitos, ≤ 12, pista «Se solicita $ 1.250.000» con `Intl es-CO`), concepto (`maxLength` 120 + contador), `CampoFoto` reutilizado como «Foto de la factura o cuenta de cobro» (`aria-label` «Factura o cuenta de cobro (opcional)»), viaja como `foto_0`. Resumen de pago (tipo, empresa, beneficiario, identificación, teléfono, monto, concepto, miniatura). `handleEnviar` arma el payload de pago; ante 4xx muestra `message` del servidor si viene, 413 y 503 con texto propio. Compra intacta.
- Pruebas: hardening 23 (11 de pago: sin monto → 400 y no toca la base; entero > 0; concepto ≤ 120; estricto por tipo; forma exacta del `create`; sin teléfono; contraseña incorrecta → 202 neutro también en pago; 409/422 con mensaje; PUBLIC_ACCESS_DENIED del servicio → 503; compra sigue 202). Portal 46 (11 de pago: bifurcación, 4 etapas, validaciones, payload exacto, foto_0, mensaje del servidor, atrás sin perder datos, compra intacta). Integración `public-payment` 6 (beneficiario nuevo pendiente + auditoría, enlace por identificación, foto ligada a la línea, 400 sin monto, contraseña mala neutra, `it.fails` ruta por empresa). E2E +2 casos de pago (recorrido completo y validaciones), en desktop y mobile.
- Verificación visual en navegador (demo, 375 y 1024 px): sin desbordamiento (0 px) en los tres pasos y el resumen.
A medias:
- Nada del alcance de S3.
Próximos pasos:
- Aplicar el «Parche para el coordinador» (abajo) y cambiar `it.fails` → `it` en `tests/integration/public-payment.test.ts`.
- Ola 3: QA del E2E #7 de la adenda con datos reales (beneficiario existente vs nuevo).
Cómo verificar:
- `npm run typecheck` → 0 errores · `npm run lint` → 0 problemas
- `npm run test -- tests/unit/public tests/integration/public` → 8 archivos, 106 pruebas verdes (incluye `public-request-portal` y `public-portal-hardening`)
- `npm run test:e2e -- public-portal` → 31 verdes, 1 omitida (la de tamaños táctiles solo corre en `mobile`)
Decisiones pendientes del usuario:
- Empresa en el paso «El pago» y no en el paso 1 (A12 literal): al elegir «Solicitud de pago» el selector desaparece del paso 1. Alternativa: dejarla en el paso 1 para los dos tipos.
- Adjunto del pago = FOTO (JPG/PNG/WebP ≤ 5 MB), no PDF: `lib/infrastructure/public-photos.ts` solo acepta imágenes por firma binaria y es `lib/**`. Si Sixteam sube facturas en PDF, hay que ampliar ese módulo (parche 3).
- Tope del monto: 1e12 en el esquema y 12 dígitos en el campo. Tope del concepto: 120.
- Errores de dominio visibles tras la contraseña correcta (409 homónimo, 422 inactivo): no abre oráculo nuevo (`POST /api/public/access` ya dice si la contraseña es correcta), pero es un cambio de postura respecto al «todo 202».
- Estados HTTP: 400 solo para forma (esquema/JSON/multipart); dominio va en 409/422 como `apiError`, y no en 400 como decía el plan literalmente.
Riesgos:
- BUG PREEXISTENTE (lib, fuera de S3): `ProcurementService.create` (procurement-service.ts:96) exige `workId` y llama `verify(workId, …)`; nunca `verifySociety`. Por la ruta general por empresa (desde 2026-09-11) lanza `PUBLIC_ACCESS_DENIED` para compra Y pago: con `main` el 202 neutro lo tapaba (requisición perdida en silencio); con S3 responde 503 «no disponible» (visible, pero sigue sin radicar hasta el parche 1). Por eso la integración de pago usa el enlace por obra y la ruta por empresa está en `it.fails`.
- `tests/e2e/authorization-and-backend.spec.ts:46` espera `[202, 503]` al `POST {}`; con S3 sería 400. Solo corre con `E2E_REAL_BACKEND=1`. No es archivo de S3 (parche 2).
- `it.fails` de la ruta por empresa se pone rojo en cuanto entre el parche 1: es a propósito (tripwire), hay que cambiarlo a `it`.
- Se cambió la expectativa de tres pruebas previas de hardening que dependían del `catch → 202`: infra tras verificar → 503; obra+empresa / sin destino → 400. Dos pruebas de orden de verificación ahora mockean `create` resuelto (`creacionCorrecta`).
- El progreso usa `style={{ gridTemplateColumns }}` en línea porque `public-request.module.css` fija 5 columnas y el CSS no está en la lista exclusiva de S3; si se prefiere, regla `.progress[data-steps="4"]` en el módulo.
- Durante la verificación había ~200 procesos `node` colgados en la máquina (arrancados entre 15:57 y 21:25 del 15-sep; parecen workers de vitest/Playwright de otras sesiones). No es de S3, pero consume RAM y puede ralentizar las suites.
Parche para el coordinador:
1. `lib/services/procurement-service.ts` `create()`, canal `publico`: `if (input.workId) ok = verify(workId, token, code); else if (input.societyId) ok = verifySociety(societyId, token, code); else deny` (hoy solo la primera rama). Después: `it.fails` → `it` en `tests/integration/public-payment.test.ts`, y si se quiere, `PUBLIC_ACCESS_DENIED` del servicio podría volver a tratarse como neutro en la ruta (hoy 503 a propósito para no perder solicitudes en silencio).
2. `tests/e2e/authorization-and-backend.spec.ts:46`: `expect([202, 503])` → `expect([400, 503])`.
3. (RF-108 «adjunto», opcional) aceptar PDF en `lib/infrastructure/public-photos.ts` (`safePhoto` + `IMAGE_MIME_TYPES`) y en `CampoFoto`/`validateAttachmentFile` del portal si Sixteam sube facturas PDF; hoy el portal solo ofrece foto.


---

# ESTADO — feat/reportes-cierre-caja
Rama y base: feat/reportes-cierre-caja desde feat/pagos-nucleo @ 35aae5f (ola 1 verificada; origin/main @ f9a7e00)
Último commit: (este commit) «MCP: registrar_pago, listar/crear centros de costo; se retira registrar_caja_menor»; anteriores: 1555834 (S5-3 reportes), 6ae6f32 (S5-2 rutas 410), 72beedb (S5-1 cierre de caja).
Hecho:
- S5-1 Cierre de caja (A10, RF-708): `components/screens/connected/expenses.tsx` es la pantalla "Cierre de caja" — pestaña por defecto con rango de fechas (lunes–viernes de la semana en curso; atajos "Esta semana"/"Este mes"), filtro por centro de costo, lista de pagos VIGENTES con medio `efectivo` (fecha, OC/OP + requisición, beneficiario, centro de costo, empresa facturada, nota, enlace al comprobante `/api/attachments/pago_orden/<pago>/<adjunto>/download`), franja de totales y "Descargar Excel" (report:export). Segunda pestaña "Libro de gastos": el libro de gastos existente con "Repartir entre obras" (RF-305), comprometido sin pagar y subtotales por etiqueta (RF-702). Se RETIRAN el formulario "Registrar gasto directo", la lista "Caja" (caja_menor), la pestaña Ingresos y el "Cierre mensual" por caja.
- `GET /api/reports/cash-close?from&to[&costCenterId][&format=xlsx]` (`app/api/reports/cash-close/route.ts` + `cash-close-report.ts`): `ProcurementService.listCashPayments` (expense:read) + nombres resueltos en servidor (`postgresReportCatalogSource`) → `toCashCloseReport` (`lib/services/report-service.ts`, pura); Excel con exceljs (report:export + auditoría `cierre_caja_descargado`).
- Menú: `/gastos` se llama "Cierre de caja" (`lib/demo-data.ts`) y lo ven Revisor, Contabilidad, Administrador Mizar (nuevo) y Administrador Sixteam (`app-shell.tsx`). `data.ts`: la ruta `/gastos` ya solo pide `/api/expenses` + catálogos.
- S5-2 Rutas retiradas: `POST /api/petty-cash`, `GET/POST /api/incomes`, `GET/POST /api/cash-closes` → 410 `{ error: "RETIRADO", message: "Los gastos de caja se registran como pagos con medio Caja sobre la orden. Ver Cierre de caja." }` (sin exigir sesión). `GET /api/petty-cash` sigue leyendo el histórico. `lib/services/cash-service.ts` y `tests/unit/cash-service.test.ts` eliminados (nadie los usaba); export quitado de `lib/services/index.ts`. Test `tests/unit/cash-routes-retired.test.ts`.
- S5-3 Reportes (RF-707): `ReportService.listOrderReport(filters, ctx)` (report:read, visibilidad del repositorio, `ListQuery` con status generada/cumplida/no_cumplida, mes → rango de `fecha_generacion`, medio/estado de pago, empresa facturada) → `OrderReportRow` (total = `calculateLineTotal` de las líneas, `paidAmount` vigente, `paymentStatus` del adaptador o derivado); nueva `GET /api/reports/orders`. `ReportRow.billedCompanyId` + filtro `billedCompanyId` en `GET /api/reports` y `/export` (columna "Empresa facturada" en el Excel de requisiciones). `reports.tsx`: filtro y columna de empresa facturada; bloque "Comprometido vs pagado" (franja comprometido/pagado/saldo/órdenes, tablas por centro de costo y por mes de la orden) con selects de medio y estado de pago junto al bloque; el compilado mensual sigue igual. Excel de gastos (`buildProvisionalHelisaXlsx`): columnas "Empresa facturada", "Estado de pago", "Pagado COP" (cruce gasto→orden en `expenses-report.ts`; caja menor histórica = pagada).
- Gasto sin obra (aviso del coordinador, N4): `expenses-report.ts` conserva en el filtro por sociedad los gastos con `workId === ""` por su `billedCompanyId` y pinta "—" en Obra; `reports.tsx`, `listOrderReport` y el cierre de caja tratan la obra vacía como ausente ("—"). Casos: `tests/unit/reports-expenses-report.test.ts`, `report-service.test.ts`, `reports-cash-close-route.test.ts`.
- S5-4 MCP (RF-1204): `app/mcp/server.ts` (`createMcpServer(actor, services)` con servicios inyectados) + `app/mcp/route.ts` (cableado de producción). Se retira `registrar_caja_menor`; entran `registrar_pago` (orderId + `orderPaymentSchema`, → `registerOrderPayment` con `{ actor, origin: "mcp" }`), `listar_centros_costo` (`soloActivos` por defecto) y `crear_centro_costo` (→ `CatalogService.create("costCenters")`, mismas reglas que `createCatalogSchema`). Aprobar/devolver/declinar siguen fuera (`assertSafeMcpToolCatalog` + `mcpForbidden`). `MCP_TOOL_NAMES` actualizado en `lib/security/mcp.ts`. Test nuevo `tests/unit/mcp-route.test.ts` (cliente MCP real sobre `InMemoryTransport`).
A medias:
- Nada.
Próximos pasos:
- Integración por el coordinador (ola 3). Si Ernesto veta A10, `git revert` de 72beedb y 6ae6f32 devuelve «Gastos y caja» completo (las tablas nunca se tocaron).
- QA con datos reales: cierre de caja con pagos anulados (no deben salir), comprobante subido después del pago, y `/gastos` para Administrador Mizar.
Cómo verificar:
- `npm run typecheck` → 0 errores; `npm run lint` → limpio.
- `npm run test -- connected-expenses-detail cash-service tests/unit/reports connected-reports report-service mcp-route cash-routes-retired` → verde (cash-service ya no existe: el patrón no matchea nada; `cash-routes-retired` cubre el 410 de `POST /api/petty-cash`).
- `npm run test` completo → verde (se corrió también aquí).
Decisiones pendientes del usuario:
- QUÉ SE RETIRÓ de «Gastos y caja» (reversible por git, tablas intactas): el "gasto directo" de caja sin requisición ni aprobación (formulario + `POST /api/petty-cash` + MCP `registrar_caja_menor`; `ProcurementService.registerPettyCash` queda sin llamador), la pestaña Ingresos (`/api/incomes` → 410), el cierre mensual por caja con saldo inicial/final (`/api/cash-closes` → 410, `CashService` borrado) y la lista "Caja" de movimientos de `caja_menor` en pantalla (`GET /api/petty-cash` sigue vivo para el histórico). Los ingresos y el saldo por caja vuelven en la fase 2 financiera (adenda §7).
- El Aprobador NO recibe el menú "Cierre de caja" aunque vea Reportes: no tiene `expense:read` en `lib/domain/rules.ts` y la ruta le respondería 403. Si Ernesto quiere que Juliana lo vea, hay que darle ese permiso (parche del coordinador) y añadir `/gastos` a su lista en `app-shell.tsx`.
- "Comprometido" excluye las órdenes `no_necesario` (su gasto se anuló); "por periodo" agrupa por el MES DE GENERACIÓN de la orden (el compromiso nace al generarla), no por fecha de pago. Si contabilidad quiere "pagado por mes de pago", es otra consulta sobre `pagos_orden`.
- El Excel de gastos (provisional Helisa) imprime la empresa facturada como id crudo, igual que ya hacía con obra/etiqueta/proveedor; el Excel de requisiciones y el del cierre sí resuelven nombres.
- El cierre de caja no se "cierra" (no hay estado cerrado/abierto ni bloqueo de edición como tenía `cierres_caja`): es una vista por rango, como pide RF-708. Bloquear altas de pagos en un rango cerrado sería una regla nueva.
Riesgos:
- Archivos tocados FUERA de la lista exclusiva de S5, todos necesarios y mínimos: `components/screens/connected/data.ts` (carga de `/gastos` sin caja menor/ingresos; sin esto la ruta cae por los 410), `lib/demo-data.ts` (etiqueta del menú), `lib/reports/types.ts` y `lib/reports/xlsx.ts` (columnas nuevas en los dos Excel), `lib/services/index.ts` (export de cash-service), `lib/security/mcp.ts` (`MCP_TOOL_NAMES`), `tests/unit/security.test.ts` (lista de tools + fake `orders.listVisibleTo`), `tests/unit/connected-data-performance.test.ts` (prueba H2 de adjuntos de caja menor sustituida), `tests/unit/connected-filters.test.tsx` (RF-703 abre la pestaña "Libro de gastos", sin filas de caja menor) y `tests/unit/operational-attachments.test.tsx` (retirado el caso del recibo del gasto directo). Nadie más de la ola 2 los toca.
- `ExpenseBundle` (shared.tsx, intocable en la ola 2) conserva `pettyCash`/`pettyAttachments`/`incomes`: `data.ts` los rellena vacíos. Limpiarlos es un parche del coordinador sobre shared.tsx.
- `lib/http/schemas.ts` sigue exportando `pettyCashSchema`/`incomeSchema`/`cashCloseSchema` (sin uso; no es mío).
- `tests/visual/routes.spec.ts` captura `/gastos` como "gastos-caja-menor": la línea base visual cambiará (pantalla nueva).
- `reports.tsx` extiende `ReportRow` localmente con `billedCompanyId` (el tipo de shared.tsx no lo declara). Cuando shared.tsx se pueda tocar, mover el campo allí.
Parche para el coordinador:
- `lib/domain/rules.ts`: si el Aprobador debe ver el cierre de caja, añadir `expense:read` a `aprobador` (y `/gastos` a su lista en `app-shell.tsx`).
- `components/screens/connected/shared.tsx`: `ReportRow.billedCompanyId?: string`; `ExpenseBundle` sin `pettyCash`/`pettyAttachments`/`incomes` (y quitar sus tipos `PettyRow`/`IncomeRow`/`CashCloseRow` si nadie más los usa); `uploadOperationalAttachment` ya no necesita `caja_menor` desde esta pantalla.
- `lib/services/procurement-service.ts`: `registerPettyCash`/`PettyCashInput` quedan inalcanzables (solo los prueba `procurement-service.test.ts`); retirarlos cuando se decida el destino final del módulo.
- `lib/http/schemas.ts`: `pettyCashSchema`, `incomeSchema`, `cashCloseSchema` sin uso.
