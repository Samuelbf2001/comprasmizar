import { ArrowRight, BarChart3, CalendarDays, CheckCircle2, Database, Inbox, Plus, Truck } from 'lucide-react';
import { requisitions } from '../../lib/demo-data';
import { DemoNotice } from '../ui/demo-notice';
import { RequestRows, SectionTitle, Tone } from './screen-primitives';

export function DashboardScreen({ go }: { go: (href: string) => void }) { return <><SectionTitle eyebrow="Lunes, 24 de agosto de 2026" title="Buenos días, Daniel" description="Este es el pulso de las compras de obra." action={<div className="title-actions"><DemoNotice compact /><button className="button button-dark" type="button" onClick={() => go('/requisiciones/nueva')}><Plus size={16} /> Nueva requisición</button></div>} /><div className="stats-grid"><Stat label="En revisión" value="12" meta="requieren normalización" icon={Inbox} tone="amber" /><Stat label="En aprobación" value="4" meta="$ 8,2 M en trámite" icon={CheckCircle2} tone="blue" /><Stat label="Compras pendientes" value="7" meta="2 llevan más de 5 días" icon={Truck} tone="orange" /><Stat label="Gasto del periodo" value="$ 42,8 M" meta="72% del presupuesto" icon={BarChart3} tone="forest" /></div><div className="dashboard-grid"><section className="panel"><div className="panel-head"><div><div className="eyebrow">Vista operativa</div><h2>Embudo de requisiciones</h2></div><CalendarDays size={18} aria-label="Periodo de demo" /></div><div className="funnel">{[['18','Recibidas','100%'],['12','En revisión','72%'],['4','En aprobación','42%'],['9','Aprobadas','58%']].map(([n,label,width]) => <div className="funnel-row" key={label}><div><span className="funnel-number">{n}</span><span className="funnel-label">{label}</span></div><div className="funnel-bar"><i style={{ width }} /></div><span className="funnel-note">demo</span></div>)}</div><button className="text-link" type="button" onClick={() => go('/reportes')}>Ver flujo completo <ArrowRight size={14} /></button></section><section className="panel panel-alerts"><div className="panel-head"><div><div className="eyebrow">Atención requerida</div><h2>Lo que no puede esperar</h2></div><Tone tone="warning">4 pendientes</Tone></div><Alert icon={<Inbox size={16} />} text="12 requisiciones esperan normalización" go={() => go('/revision')} /><Alert icon={<Truck size={16} />} text="2 órdenes sin confirmación de entrega" go={() => go('/ordenes')} /><Alert icon={<Database size={16} />} text="1 ítem espera normalización" go={() => go('/catalogos')} /></section></div><section className="panel recent-panel"><div className="panel-head"><div><div className="eyebrow">Actividad reciente · demo</div><h2>Últimas requisiciones</h2></div></div><RequestRows rows={requisitions.slice(0, 4)} onOpen={id => go(`/requisiciones/${id}`)} /></section></>; }
function Stat({ label, value, meta, icon: Icon, tone }: { label: string; value: string; meta: string; icon: typeof Inbox; tone: string }) { return <div className={`stat-card stat-${tone}`}><span className="stat-icon"><Icon size={17} /></span><span className="stat-label">{label}</span><strong>{value}</strong><span className="stat-meta">{meta}</span></div>; }
function Alert({ icon, text, go }: { icon: React.ReactNode; text: string; go: () => void }) { return <button className="alert-item" type="button" onClick={go}><span className="alert-icon amber">{icon}</span><span><strong>{text}</strong><small>Estado de demostración</small></span><ArrowRight size={15} /></button>; }
