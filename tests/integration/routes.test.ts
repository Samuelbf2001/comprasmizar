import { describe, expect, it } from "vitest";
import { GET as health } from "../../app/api/health/route";
import { POST as publicRequest } from "../../app/api/public/requisitions/route";
import { POST as kapso } from "../../app/api/kapso/route";
import { GET as requisitions } from "../../app/api/requisitions/route";
import { GET as dashboard } from "../../app/api/dashboard/route";
import { GET as catalogs } from "../../app/api/catalogs/route";
import { GET as catalogManagement } from "../../app/api/catalogs/manage/route";
import { GET as expensesReport } from "../../app/api/reports/expenses/route";
import { GET as orderDocument } from "../../app/api/orders/[id]/document/route";
import { GET as suppliers, POST as createSupplier } from "../../app/api/suppliers/route";
import { GET as supplierDetail } from "../../app/api/suppliers/[id]/route";
import { POST as supplierDocument } from "../../app/api/suppliers/[id]/documents/route";
import { POST as completeSupplierDocument } from "../../app/api/suppliers/[id]/documents/[documentId]/complete/route";
import { GET as downloadSupplierDocument } from "../../app/api/suppliers/[id]/documents/[documentId]/download/route";
import { GET as attachments, POST as prepareAttachment } from "../../app/api/attachments/[entity]/[entityId]/route";
import { POST as completeAttachment } from "../../app/api/attachments/[entity]/[entityId]/[attachmentId]/complete/route";
import { GET as downloadAttachment } from "../../app/api/attachments/[entity]/[entityId]/[attachmentId]/download/route";

describe("routes fail closed without runtime credentials", () => {
  it("does not expose demo data when dependencies are absent", async () => { expect((await health()).status).toBe(503); const publicResponse = await publicRequest(new Request("http://localhost/api/public/requisitions", { method: "POST", body: "{}" })); expect(publicResponse.status).toBe(503); const kapsoResponse = await kapso(new Request("http://localhost/api/kapso", { method: "POST", body: "{}" })); expect(kapsoResponse.status).toBe(503); expect((await requisitions()).status).toBe(503); expect((await dashboard(new Request("http://localhost/api/dashboard?period=2026-08"))).status).toBe(503); expect((await catalogs()).status).toBe(503); expect((await catalogManagement()).status).toBe(503); expect((await expensesReport(new Request("http://localhost/api/reports/expenses?period=2026-08"))).status).toBe(503); expect((await orderDocument(new Request("http://localhost/api/orders/x/document"), { params: Promise.resolve({ id: "x" }) })).status).toBe(503); expect((await suppliers()).status).toBe(503); expect((await createSupplier(new Request("http://localhost/api/suppliers", { method: "POST", body: "{}" }))).status).toBe(503); expect((await supplierDetail(new Request("http://localhost/api/suppliers/x"), { params: Promise.resolve({ id: "x" }) })).status).toBe(503); expect((await supplierDocument(new Request("http://localhost/api/suppliers/x/documents", { method: "POST", body: "{}" }), { params: Promise.resolve({ id: "x" }) })).status).toBe(503); expect((await completeSupplierDocument(new Request("http://localhost/api/suppliers/x/documents/y/complete", { method: "POST", body: "{}" }), { params: Promise.resolve({ id: "x", documentId: "y" }) })).status).toBe(503); expect((await downloadSupplierDocument(new Request("http://localhost/api/suppliers/x/documents/y/download"), { params: Promise.resolve({ id: "x", documentId: "y" }) })).status).toBe(422); const entityId = "11111111-1111-4111-8111-111111111111", attachmentId = "22222222-2222-4222-8222-222222222222", attachmentParams = Promise.resolve({ entity: "requisicion", entityId }); expect((await attachments(new Request("http://localhost/api/attachments/requisicion/x"), { params: attachmentParams })).status).toBe(503); expect((await prepareAttachment(new Request("http://localhost/api/attachments/requisicion/x", { method: "POST", body: "{}" }), { params: attachmentParams })).status).toBe(503); expect((await completeAttachment(new Request("http://localhost/api/attachments/requisicion/x/y/complete", { method: "POST", body: "{}" }), { params: Promise.resolve({ entity: "requisicion", entityId, attachmentId }) })).status).toBe(503); expect((await downloadAttachment(new Request("http://localhost/api/attachments/requisicion/x/y/download"), { params: Promise.resolve({ entity: "requisicion", entityId, attachmentId }) })).status).toBe(503); expect((await downloadAttachment(new Request("http://localhost/api/attachments/orden/x/y/download"), { params: Promise.resolve({ entity: "orden", entityId: "x", attachmentId: "y" }) })).status).toBe(422); });
});
